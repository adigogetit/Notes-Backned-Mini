<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes App</title>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 min-h-screen flex items-center justify-center">

    <div class="w-full max-w-md bg-white rounded-xl shadow-lg p-6 h-[480px] flex flex-col">
        <!-- Heading -->
        <h1 class="text-2xl font-bold text-center mb-4">Notes</h1>

        <!-- Add Note -->
        <div class="mb-4">
            <form action="/create" method="post">
                <input type="text" placeholder="Title" name="title"
                    class="w-full mb-2 p-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400" />

                <textarea rows="3" placeholder="Write your note..." name="details"
                    class="w-full p-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-400"></textarea>

                <button class="mt-3 w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition">
                    Add Note
                </button>
            </form>
        </div>

        <!-- Notes List -->
        <div class="space-y-4 max-h-60 overflow-y-auto pr-1">

            <% if (files.length> 0) { %>
                <% files.forEach(function(note) { %>
                    <div class="p-3 bg-gray-50 border rounded">
                        <h2 class="font-semibold">
                            <%= note.replace(".txt", "") %>
                        </h2>
                        <a class="text-sm text-gray-600" href="/file/<%=note%>">Read more</a>
                    </div>
                <%}) %>
            <% }%>

        </div>
    </div>

</body>

</html>